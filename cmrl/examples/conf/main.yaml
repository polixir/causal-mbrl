defaults:
  - algorithm: mopo
  - dynamics: plain_dynamics
  - task: boundary_IP_swing_up
  - _self_

seed: 0
device: "cuda:0"
log_frequency_agent: 1000
save_video: false
debug_mode: false

experiment: debug

root_dir: "./exp"
hydra:
  run:
    dir: ${root_dir}/${algorithm.name}/${dynamics.name}/${experiment}/${task.env}/${now:%Y.%m.%d}/${now:%H%M%S}
  sweep:
    dir: ${root_dir}/${algorithm.name}/${dynamics.name}/${experiment}/${task.env}/${now:%Y.%m.%d}/${now:%H%M%S}
  job:
    chdir: true